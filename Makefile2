# Makefile for Lower Triangular solve

# *****************************************************
# Variables to control Makefile operation

CPP = g++
CPPFLAGS = -Wall -O3 -fopenmp

# ****************************************************

starter: starter.cpp starter.h
	$(CPP) $(CPPFLAGS) starter.cpp -o starter 

clean:
	rm starter
